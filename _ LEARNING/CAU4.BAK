uses crt;
var m,n,t,i,j:integer;
    a:array[1..100,1..100] of integer;
    inp,out:text;
    {m,i=doc, n,j=ngang}
function dem(doc,ngang):integer;
var i,j:integer;
begin
     {nhap}
     clrscr;
     assign(inp,'CAU4.inp'); reset(inp);
     assign(out,'CAU4.out'); rewrite(out);

     read(inp,m,n,t);
     for i:=1 to m do
         for j:=1 to n do
             read(inp,a[i,j]);




     readln
end.